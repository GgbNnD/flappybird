# Flappy Bird Q-learning 实验报告

## 1. 任务概述

本项目使用表格型 Q-learning 为 Flappy Bird 训练智能体。代码补全位于 `src/q_learning.py`，训练实验入口位于 `src/experiment.py`，最佳模型保存为 `q_best.pkl`。

本次没有修改环境原始奖励结构，只保留代码框架中的死亡惩罚放大：环境死亡奖励 `-1` 在训练时改为 `-1000`，用于强化“避免死亡”的学习信号。

## 2. Q-learning 实现

`GameAI` 使用字典保存 Q-Function，键为 `(tuple(state), action)`，值为该状态动作对的 Q 值。每一步从环境采样 `(s, a, s', r)` 后，按以下公式更新：

```text
Q(s, a) <- Q(s, a) + alpha * (reward + gamma * max_a' Q(s', a') - Q(s, a))
```

动作空间为 `{0, 1}`，其中 `0` 表示不扇动，`1` 表示 flap。训练时使用 epsilon-greedy 策略，在探索概率 `epsilon` 下随机动作，否则选择当前 Q 值最大的动作；测试时关闭探索，只使用当前最优动作。

状态表示由 `process_obs` 完成。根据最近未通过的管道，提取并离散化四个特征：

| 状态特征 | 含义 |
| --- | --- |
| `pipe_x - player_x` | 小鸟到最近管道的水平距离 |
| `player_y - gap_center` | 小鸟到管道缺口中心的垂直差 |
| `pipe_bottom - player_y` | 小鸟到下管道顶部的垂直差 |
| `player_v` | 小鸟垂直速度 |

这些连续观测值乘以 `obs_mul_factor` 后取整，用作 Q 表 key。实验中比较了 `obs_mul_factor=24/30/36`，最佳模型使用 `30`。

## 3. 参数实验与加速方式

实验脚本共初筛 18 组参数，每组训练 30000 局并评估 20 局；选择初筛均分最高的 3 组再训练 100000 局，并评估 50 局。

机器搭载 RTX 4060 Laptop GPU。由于 Gym 环境步进和表格 Q 表更新主要是 CPU/Python 逻辑，不能直接把环境交互整体迁移到 CUDA。代码中保留了 `ThreadPoolExecutor` 线程后端；完整预算实验为了绕开 Python GIL 长尾，使用 `process` 后端完成并行训练。CUDA/PyTorch 用于候选模型的张量化统计与排序，最终记录中 `selection_device` 为 `cuda`。

为避免优秀策略在训练中出现超长单局导致实验无法结束，训练阶段设置每局最多 1000 步，评估阶段设置每局最多 5000 步。

## 4. 实验结果

初筛前三名如下：

| trial | alpha | gamma | epsilon | obs_mul_factor | 初筛均分 | 最高分 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| baseline | 0.7 | 0.95 | 0.00 | 30 | 73.90 | 132 |
| a05_g090_e005_m30 | 0.5 | 0.90 | 0.05 | 30 | 54.85 | 132 |
| a06_g099_e005_m30 | 0.6 | 0.99 | 0.05 | 30 | 49.45 | 132 |

复训结果如下：

| trial | alpha | gamma | epsilon | obs_mul_factor | 复训均分 | 最低分 | 最高分 | 标准差 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| baseline | 0.7 | 0.95 | 0.00 | 30 | 90.52 | 3 | 132 | 46.38 |
| a05_g090_e005_m30 | 0.5 | 0.90 | 0.05 | 30 | 82.48 | 2 | 132 | 47.13 |
| a06_g099_e005_m30 | 0.6 | 0.99 | 0.05 | 30 | 51.02 | 4 | 132 | 35.56 |

最佳模型为 `baseline`，已保存为 `q_best.pkl`。完整结果见 `results/experiment_results.csv` 和 `results/best_summary.json`。

## 5. 参数影响分析

`epsilon=0` 的 baseline 表现最好，说明在当前离散状态表示和固定随机种子训练下，较稳定的贪心更新可以快速积累有效 Q 值。加入 `epsilon=0.05` 后，部分参数仍能达到较高分，但均分略低；`epsilon=0.10` 的多数结果明显下降，说明探索过多会在 Flappy Bird 这种死亡惩罚很大的环境中带来较多负样本。

折扣因子方面，`gamma=0.95` 在最佳模型中表现最好，能兼顾当前生存奖励和未来过管收益。`gamma=0.99` 对未来奖励更敏感，但在部分组合中学习更不稳定。学习率方面，`alpha=0.7` 的 baseline 收敛效果最好，`alpha=0.5/0.6` 也有可用表现，但最终均分低于 baseline。

`obs_mul_factor=30` 的状态粒度最合适。`24` 会让状态过粗，丢失管道与小鸟位置差异；`36` 会让状态空间更稀疏，同样训练局数下 Q 表覆盖不足，因此整体分数偏低。

## 6. 结论

本项目完成了 Q-learning 代码补全、PDF 转 Markdown、参数搜索、最佳模型保存和实验报告。最佳 Q 表模型在 50 局评估中的平均分为 `90.52`，最高分为 `132`，相比作业说明中的默认示例平均分 `32.2` 有明显提升。
